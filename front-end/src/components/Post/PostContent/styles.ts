import styled from "styled-components";
import { colors } from "../../../constants/colors";

export const ContentContainer = styled.div`
  padding: 1rem 0;
`;

export const ContenteText = styled.span`
  letter-spacing: 0.5px;
  line-height: 2rem;
  color: ${colors.dark};
`;
